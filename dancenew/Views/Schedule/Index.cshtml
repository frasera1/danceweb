@model ScheduleVM

@{
    ViewBag.Title = "Index";
}

<section class="pageheader-default text-center" style="background:url(/Content/Images/studio.jpg);">
    <div class=" semitransparentbg">
    <h1 class="animated fadeInLeftBig notransition">Zeitplan</h1>
    <p class="animated fadeInRightBig notransition container page-description">
        Diesen schönen Raum können Sie für Kurse und Workshops mieten. <br />
        Kontaktieren Sie mich füer weitere Informationen.
    </p>
    </div>
</section>
<div class="wrapsemibox">
    <div class="semiboxshadow text-center">
        <img src="~/Content/Template/img/shp.png" class="img-responsive" alt="">
    </div>
    <!-- FAQs
    ================================================== -->
    <section class="container animated fadeInUp notransition">
        <!-- 1ST ROW
        ================================================== -->
        <div class="row">
            <!-- BLOCK 1 -->
            <div class="container-fluid col-md-6">
                <h3 class="text-center">ERWACHSENE</h3>
                <div class="col-md-12">
                 
                    <h5>Ballett für Anfänger:</h5>
                    <table class="table table-striped table-responsive">
                        @foreach (var item in Model.AdultsAdvanced)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Weekday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndTime)
                                </td>
                            </tr>
                        }
                            
                    </table>
                    
                    <h5>Ballett für Fortgeschrittene:</h5>
                    <table class="table table-striped table-responsive">
                        @foreach (var item in Model.AdultsBeginner)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Weekday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndTime)
                                </td>
                            </tr>
                        }

                    </table>
                    
                    <h5>Modern Dance:</h5>
                    <table class="table table-striped table-responsive">
                        @foreach (var item in Model.AdultModern)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Weekday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndTime)
                                </td>
                            </tr>
                        }

                    </table>
                </div>
                
            </div>
            <!-- end block 1 -->
            <!-- BLOCK 2 -->
            <div class="container-fluid col-md-6">
                <h3 class="text-center">KINDER</h3>
                <div class="col-md-12">

                    <h5>Kinderklassen Kreativer Tanz (3-4 Jahre):</h5>
                    <table class="table table-striped table-responsive">
                        @foreach (var item in Model.Kids34)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Weekday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndTime)
                                </td>
                            </tr>
                        }

                    </table>

                    <h5>Kreatives Ballett (5-7 Jahre):</h5>
                    <table class="table table-striped table-responsive">
                        @foreach (var item in Model.Kids57)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Weekday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndTime)
                                </td>
                            </tr>
                        }

                    </table>

                    <h5>Kinderklassen Kreativer Tanz In English (3-4 Jahre):</h5>
                    <table class="table table-responsive table-striped">
                        @foreach (var item in Model.Kids34Eng)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Weekday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.StartTime)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EndTime)
                                </td>
                            </tr>
                        }

                    </table>
                </div>

            </div>
            <!-- end block 2 -->
        </div>
        <!-- 2ND ROW
        ================================================== -->
        
    </section>
    <!--CALL TO ACTION PANEL
    ================================================== -->
    
    <!-- /. end call to action-->
</div>
